# SQL Workspace

This folder contains starter SQL for both healthcare portfolio projects.

## Intended Use

- create staging tables after downloading source files
- normalize fields and rename columns
- generate KPI-ready outputs for Power BI

## Current State

These scripts are templates designed to speed up project setup.

After the source files are downloaded, update column names and data types to match the real extracts.
